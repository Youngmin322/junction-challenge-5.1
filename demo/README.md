# 한울원전 조건부 접근 우선순위 지도

한울원전의 실제 위치 맥락 위에 **더미 관측 군집과 합성 해류**를 올려, 계산 흐름을 설명하는 해커톤용 웹 데모입니다. 기본 시나리오는 울진 동해안 외해의 군집이 서쪽 취수구 감시선으로 접근하도록 만들어졌습니다.

> 한울원전 위치를 제외한 관측시각·군집·해류·수심 가정·곡선 접근영역은 모두 시연용 합성 데이터입니다. 화면의 방향·유속·영역은 실제 막힘 확률, 생물량 또는 시설 위험도를 뜻하지 않습니다.

## 실행

```bash
npm install
npm run demo
```

브라우저에서 Vite가 출력하는 주소를 엽니다. 배포 번들은 `npm run demo:build`로 생성합니다.

## 화면 읽는 법

해류 시점, 모양, 색은 다음처럼 연결됩니다.

- 해류 시점 슬라이더: 해당 시점의 합성 `u/v`를 선택하며 영역을 숨기지 않음
- 해류 방향: 취수구에서 시작해 각 위치의 `u/v`를 반복 표본화하며 상류 방향 곡선을 생성
- 공간 변화: 위치마다 다른 유향이 중심선을 휘게 하고, 각 지점의 접선에 맞춰 띠와 화살표를 배치
- 빠른 유속: 더 멀리 연결되도록 길어지고, 강한 방향성을 표현하도록 펼침각이 좁아짐
- 느린 유속: 짧아지고 펼침각이 넓어짐
- 색: 전체 곡선 길이의 `0–12%` / `12–30%` / `30–65%` / `65–100%`

네 색 구간은 모든 시점에 항상 존재합니다. 진한 적색은 취수구에 가장 가까운 상대 구간이고, 옅은 살구색은 합성 해류의 상류 쪽 끝입니다. 이 곡선 띠는 **선택한 해류 아래의 설명용 조건부 접근영역**입니다.

계산 순서는 `취수구 → 현재 위치의 해류 조회 → 유향의 반대 방향으로 한 스텝 이동 → 새 위치에서 다시 조회`입니다. 이 과정을 40회 반복해 중심선을 만들고, 유속으로 전체 길이와 펼침각을 정한 뒤 중심선 양옆에 네 거리 구간을 생성합니다. 첨부된 KHOA 동해 표층 해류 PDF는 곡선 흐름과 공간별 화살표 배치의 시각 참고이며, PDF 자체를 수치 해류 데이터로 읽어 계산하지는 않습니다.

## 제공 시나리오

- `한울원전 접근 흐름`: 실제 한울 위치 맥락 + 서쪽을 향하는 합성 해류
- `동쪽 확산`: 일정한 동북동 가상 흐름
- `조류 반전`: 4시간 후 남동쪽으로 흐름 변화
- `해안 차단`: 가상 해안선·얕은 수역에서 일부 입자 이동 중단

## 실제 데이터로 교체하는 경계

지도는 해류 API를 직접 호출하지 않습니다. [scenarios.ts](./src/scenarios.ts)가 엔진의 `SimulationInput`을 만들고, 지도는 계산된 GeoJSON만 받습니다. 따라서 아래 provider를 실제 어댑터로 바꿔도 렌더러는 그대로 사용할 수 있습니다.

`offshoreFlow.velocityAt`이 취수구 또는 역추적 중간 위치에서 `null`을 반환하면, 데모는 이전 벡터를 재사용해 영역을 만들어내지 않습니다. 해당 시점의 곡선영역과 화살표를 비우고 **해류 데이터 범위 부족** 상태를 표시합니다.

| 입력 | 실제 어댑터의 책임 | 연결 위치 |
|---|---|---|
| 관측 군집 | 시각·점/폴리곤·종·수심·신뢰도를 정규화 | `seed` |
| 외해 해류 | 시각·위치·수심에 맞는 `u/v` 반환 | `offshoreFlow.velocityAt` |
| 연안 조류 | 연안 영역에서 외해장을 교체하거나 확인된 잔차만 합산 | `nearshorePolicy` |
| 수심·해안선 | 다음 입자 위치의 통과 가능 여부 판정 | `navigability.canTraverse` |
| 감시 게이트 | 취수구 전면 선분 또는 중심·폭 정의 | `gate` |

해류와 연안 조류를 무조건 더하면 조석이 중복될 수 있습니다. 실제 연동 시 `replace` 또는 `residualAdd` 정책을 원본 모델 정의에 맞게 선택해야 합니다.

## 지도 출력 계약

- `traceUpstreamCurrentCenterline(...)`: `velocityAt(위치)`를 반복 호출해 취수구 기준 상류 곡선 중심선 생성
- `buildCurrentOrientedFanEnvelope(...)`: 중심선 접선과 유속으로 항상 네 구간을 가진 곡선 띠 생성
- `earliestArrivalBands`: 격자별 최초 도달 시간
- `buildApproachPriorityBands(...)`: 위 격자에 취수구 거리와 `approachPriority` 추가
- `particleTrajectories`: 앙상블 입자 경로
- `horizonSummaries`: 2/4/24/48시간 조건부 게이트 연결 비율과 ETA p10/p50/p90
- `snapshots`: 시간별 동적 연결영역

웹 데모는 곡선 접근띠 출력을 사용하고, 입자 기반 출력은 향후 정밀 계산·검증용으로 유지합니다. 실제 API가 붙어도 `offshoreFlow.velocityAt`이 위치·시각·수심별 `u/v`를 반환하면 같은 렌더러를 재사용할 수 있습니다.
